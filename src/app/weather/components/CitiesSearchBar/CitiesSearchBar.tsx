import React from "react";
import { Search } from "semantic-ui-react";
import "./search-bar.scss";

const CitiesSearchBar: React.FC = () => {
  return <Search size="small" className="search-bar" />;
};

export default CitiesSearchBar;
